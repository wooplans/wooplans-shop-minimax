name = "wooplans-checkout"
main = "src/index.ts"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# Default product IDs (can be overridden per request)
VILLA_PRODUCT_ID = "prd_n770b0"
DUPLEX_PRODUCT_ID = "prd_krnnh9"

# Chariow API base URL
CHARIOW_API_URL = "https://api.chariow.com/v1"
